# Ignore node_modules
node_modules/

# Ignore compiled files
*.exe
*.dll

# Ignore environment files
.env# Start the SSH agent if it's not already running
eval $(ssh-agent -s)

# Add your private key to the agent
ssh-add ~/.ssh/id_rsa
